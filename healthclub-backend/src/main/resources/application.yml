spring:
  profiles:
    active: "dev"

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/healthclub
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  logging:
    level:
      ROOT: INFO

---
spring:
  config:
    activate:
      on-profile: prod
